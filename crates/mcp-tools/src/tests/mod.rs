//! Unit tests for the rmcp-free helper layer. Kept in a dedicated `src/tests/`
//! folder per project convention (not inline `#[cfg(test)]` in source).

mod args;
mod fake;
mod flatten;
mod format;
mod handlers;
mod response;
